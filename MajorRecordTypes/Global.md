# Major Record Type: Global

Global records are fro storing numeric data (everything is converted to a float for storage). 

## Flags

Global records in starfield only have 1 exposed flag Constant. 

### Constant

The Constant flag makes the value read-only at runtime, so papyrus scripts and the console cannot be used to modify their value.
